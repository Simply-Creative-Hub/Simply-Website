{
  "name": "app1",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["web", "desktop", "backend"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
